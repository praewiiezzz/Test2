<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/relativelayout"
    android:padding="22dp"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" >

    <ImageView
        android:id="@+id/rotateImage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/counterclockwise_yellow"
        android:adjustViewBounds="true"
        android:maxWidth="200dp"
        android:maxHeight="200dp"
        android:layout_centerHorizontal="true"
        android:scaleType="fitCenter"/>

    <TextView
        android:id="@+id/Heading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@id/rotateImage"
        android:layout_alignTop="@id/rotateImage"
        android:layout_alignRight="@id/rotateImage"
        android:layout_alignBottom="@id/rotateImage"
        android:layout_margin="1dp"
        android:textStyle="bold"
        android:text="0.0"
        android:gravity="center"
        android:textColor="#ffffff"
        android:textSize="35dp" />

</RelativeLayout>